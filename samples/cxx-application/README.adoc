= application with cxx sources

This sample demonstrates an application with C++ sources.

Look for source files in `src/main`.

To build this sample, run: `../../gradlew build`

Look for the executable file in `build/exe`.